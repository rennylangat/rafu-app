import 'package:flutter/material.dart';

class RentPaymentScreen extends StatelessWidget {
  const RentPaymentScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
